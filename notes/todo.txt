This is a file for smaller todos (see next-steps.txt for master plan and features.txt for bigger plans)
For bugs/fixes rather than features, see bugs.txt

 * "Recipe randomization" through local switches rather than as a complete randomization
 * Check on tooltips for new randomizations
 * Account for ability to move around on planets in logic (need ice platforms on Aquilo), maybe just a compat on ice platforms etc.?
 * Branched randomization; military part of tech tree being separate for example (might make randomization process too sensitive)
 * Spoiling checks on buildings (make sure it's not too quick)
 * Force space resources in appropriate places (like planetary science packs)
 * Randomized top sorts in new sorts (make random preferred order and sort according to that?)
 * Address recipe tech unlock scattering with bubbling
 * Randomized settings setting
 * Think about possibility/helpfulness of backtracking?
 * Softlock checking based on concrete canonical nodes, better error messages for softlocks
 * Add recipetweaker to list of recommended mods, and consider other "tweaker" mods
 * Think about secondary sort rando (doing a second sort and taking intersection to reduce linearity)
 * Check heating_energy when checking whether things can freeze (zero means it can't freeze)
 * Document that ability to mine fluid ("uranium mining") is researched from start
 * Limits setting
    * Go through orb and dosh run to see what needs to be changed with regards to limitation
 * Tech trigger handler randomization
 * Presets to guide player away from "ultimate chaos"
 * Fail on softlocks during testing (and perhaps also on prereq shuffle failures by searching for % or something)
 * Listen for GUI changes and change randomizer panel scale based on that (and clean up GUI code in general)
 * Move contexts to logic utils (or even context-utils?)
 * Reorganize new-lib better, in particular separate graph lib and sort lib as that part is growing too large (and maybe figure out where an "important node decider"/other graph logical analysis would go)
 * Progression nodes/ability to decide when a node/pebble is "important"
 * Starting "windmill" to get over initial problematic power hump; maybe something for space hump as well?
 * Support additional_categories for recipes in logic
 * Logic for not being able to handcraft in space (if not already there)
 * Check that agri towers are counted as automatable on gleba
 * Only add automatable for resource mining if it's by a mining drill
 * Fix connection logic that adds contexts to a node once it's connected in first pass (not quite correct)
 * Don't use balance nodes in control phase reachability checks
 * Enforce .nodes and .edges access?
 * Work on explorer:
    * Explain the square to select prototypes
    * More gaps/padding
    * Resize home/explorer tab main flows to take up whole window
    * Right clicking on prototype causes crash
    * Close button
    * https://discord.com/channels/139677590393716737/231518135231053825/1479302758778667172
 * Make sure to return "not stackable" on vehicles with equipment grid defined, not just armor
 * Change formula so infinite researches can become exponential or become linear cost scaling
 * Randomizations localised descriptions for created entities, such as a laser turret's beam, should also go with the laser turret, for example
 * Remove hardcoding in module randomizations
 * Just make furnaces into assembling machine prototypes to avoid issues (most things that would break in mods like voiding machines need manual compat for their recipes anyways)
 * Voiding recipes (toggleable with setting)
 * Technology name rich text with sciences required
 * Fix technology icons to fit recipes unlocked when possible