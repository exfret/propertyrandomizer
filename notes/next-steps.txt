CURRENTLY ON:

 * Before next release, rocket launch products and node adding API

 * exfretpelago
    * (DONE) Duplicate recipes (not items), and randomize them separately in each copy
    * (DONE) Also run item rando separately in each copy, but try to link items together afterward
    * (DONE) Duplicate techs
    * (DONE) Perform new-execute randomization on every tech at once, and on tech unlocks
    * (DONE) Add back numeric icons for recipes (just recipes)
    * (DONE) Encode separate player forces as separate contexts by creating new nauvis copies and putting player's corresponding resources/recipes tied to that planet (via surface conditions)
       * Think about how to add requirement of a tech being unlocked on its corresponding planet to logic (probably duplicate science packs as well)
       * Actually, this isn't necessary, since the items can't be transferred!
    * (DONE) Write control-stage code to create new forces/surfaces, and randomly rotate one person around every 30 minutes
    * (DONE) Enable recipes on unlock for only the correct force
    * (DONE) Make map gen seed different per nauvis copy
    * Duplicate rocket silos
    * Make rocket silo change in inventory if transferred

 * Recipe ingredients unified (basic version first?)
    * Cost implementations
       * (DONE) Factor out cost functions to recipe-cost.lua
       * (DONE) Add major raw resource table and raw resource cost table to compat/vanilla.lua
       * (DONE) Calculate vanilla material-recipe costs on recipe-ingredients.lua init
       * (DONE) Filter out prereqs without any cost
       * (DONE) Calculate vanilla staged costs at beginning of custom prereq search (actually, might make more sense along with randomized costs)
       * (DONE) Calculate randomized staged costs as we go (might need to wait until next step)
       * Assign recipes to surfaces (wait no, might just need to check sort_for_pool contexts); also, might want to check all contexts hrm
       * Figure out how many times to add prereqs to shuffled prereqs (might need to allow adding multiple times again)
       * Undo context switching approach for new recipe-ingredients rando?
    * Search loop
       * (DONE) Adapt execute-new loop
       * (DONE) Reachability subroutine
    * Fixes
       * Increase complexity of ings
       * Decrease amounts (Idea: prefer just decreasing amounts afterward and making the thing just naturally cheaper)
 * Rerolls feature!
 * execute-new handler options
    * Write context switching wiki page to describe problems I'm having to others better
    * Support for recipe blacklisted pre again
    * Move recipe tech unlock node rando back so that prereqs are actual techs (can this be done? Might need new algorithmic tech)
    * (DONE) First pass specific blacklist
 * Extracting dependency graph library
 * First pass sort improvements
    * Better compatibility tests (if a node gets military science and thus an extra prereq, it can make the rest of the graph seem invalid)
    * Add more testing/logging?
